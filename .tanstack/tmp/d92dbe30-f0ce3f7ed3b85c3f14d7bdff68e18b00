import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/deposits/manual-deposit')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/deposits/manual-deposit"!</div>
}
